<video
    class="lazy-video {{ class }}"
    muted
    playsinline
    loop
    preload="metadata"
    data-src="{{ video_url.sources[0].url }}"
    poster="{{ video_url.preview_image | image_url: width: 1280 }}">
</video>